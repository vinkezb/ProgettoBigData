package classes

case class Wrapper_(
                    actor: Actor,
                    payload: Payload,
                    created_at: String,
                    id: String,
                    org: Org,
                    repo: Repo,
                      `type`: String
)
